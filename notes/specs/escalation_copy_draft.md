## Safety-Critical Escalation Copy

This note pins the exact line for low-applicability safety-critical states.

Use this exact sentence in both `query.py` and `OfflineAnswerEngine.java`:

`If this is urgent or could be a safety risk, stop and call local emergency services now (911 where applicable); if this may be poisoning, call Poison Control now, and keep the person with a trusted adult while waiting.`

Placement:
- prepend it above the `Closest matches in the library:` block for safety-critical abstain
- reuse the same line for future safety-critical uncertain-fit copy
